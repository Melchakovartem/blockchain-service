
  <h2>Token transfers</h2>         
  <table class="table table-striped">
    <tr>
     <th>Tx Hash</th>
     <th>From</th>
     <th>To</th>
     <th>Amount(WT)</th>
    </tr>
    <% transfers.each do |t| %>
    <tr>
      <td class="col-md-1"><%= link_to t.tr_hash[0..15] + "...", transaction_path(t.tr_hash) %> </td>
      <td><%= link_to t.from, address_path(t.from) %> </td>
      <td><%= link_to t.to, address_path(t.to) %></td>
      <td><%= t.amount.to_f/10**18 %> </td>
     </tr>
    <% end %>
</table>